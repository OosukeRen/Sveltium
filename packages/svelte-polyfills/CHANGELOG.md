# Changelog

## 0.1.1

### Added

- README with polyfill inventory and usage examples
- Author metadata in package.json
- This changelog

## 0.1.0

Initial release with:

- `dom.js` — DOM API polyfills for legacy Chromium (ChildNode, ParentNode, matches, closest, CustomEvent, classList.toggle)
- `proxy.min.js` — minimal ES6 Proxy polyfill (get/set/apply/construct traps)
- `index.js` — barrel import for both modules
